/**
 * @author Alex
 * @date 2020/01/05
 */
const TAG = "MentionSearchAction";
export default class MentionSearchAction {
    searchUsers(pageNum: number) {
        return async dispatch => {
        }
    }

    searchUsersLoadMore(pageNum: number, oldData) {
        return async dispatch => {
        }
    }
}
